<nav class="bg-paper-100/90 backdrop-blur border-b border-ink-900/5 sticky top-0 z-40">
  <div class="max-w-6xl mx-auto px-4 sm:px-6">
    <div class="flex items-center justify-between h-16">
      <a href="/" class="flex items-center gap-2.5 font-display font-bold text-ink-900 text-lg">
        <span class="inline-flex items-center justify-center w-9 h-9 rounded-xl bg-gradient-to-br from-brand-500 to-brand-700 text-white shadow-soft">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none"><path d="M4 6h9M4 12h16M4 18h12" stroke="currentColor" stroke-width="2" stroke-linecap="round"/><circle cx="20" cy="6" r="2.2" fill="currentColor"/></svg>
        </span>
        <span>AI Course<span class="text-brand-600">Gen</span></span>
      </a>

      <% if (currentUser) { %>
        <div class="hidden md:flex items-center gap-1">
          <a href="/courses" class="px-3 py-2 rounded-lg text-sm font-medium transition <%= currentPath.startsWith('/courses') ? 'bg-brand-50 text-brand-700' : 'text-ink-700/70 hover:bg-ink-900/5' %>">Dasbor</a>
          <a href="/outlines" class="px-3 py-2 rounded-lg text-sm font-medium transition <%= currentPath.startsWith('/outlines') ? 'bg-brand-50 text-brand-700' : 'text-ink-700/70 hover:bg-ink-900/5' %>">Outline</a>
          <a href="/chatbot/history" class="px-3 py-2 rounded-lg text-sm font-medium transition <%= currentPath.startsWith('/chatbot') ? 'bg-brand-50 text-brand-700' : 'text-ink-700/70 hover:bg-ink-900/5' %>">Riwayat Chatbot</a>
          <a href="/profile" class="px-3 py-2 rounded-lg text-sm font-medium transition <%= currentPath.startsWith('/profile') ? 'bg-brand-50 text-brand-700' : 'text-ink-700/70 hover:bg-ink-900/5' %>">Profil</a>
        </div>
        <div class="hidden md:flex items-center gap-3">
          <span class="text-sm text-ink-700/60">Halo, <strong class="text-ink-900 font-semibold"><%= currentUser.name %></strong></span>
          <form action="/auth/logout" method="POST">
            <button type="submit" class="btn btn-secondary btn-sm">Keluar</button>
          </form>
        </div>
        <button id="nav-toggle" class="md:hidden w-10 h-10 flex items-center justify-center rounded-lg hover:bg-ink-900/5" aria-label="Buka menu" aria-expanded="false">
          <svg id="nav-icon-open" width="22" height="22" viewBox="0 0 24 24" fill="none"><path d="M4 7h16M4 12h16M4 17h16" stroke="currentColor" stroke-width="2" stroke-linecap="round"/></svg>
          <svg id="nav-icon-close" class="hidden" width="22" height="22" viewBox="0 0 24 24" fill="none"><path d="M6 6l12 12M18 6L6 18" stroke="currentColor" stroke-width="2" stroke-linecap="round"/></svg>
        </button>
      <% } else { %>
        <div class="flex items-center gap-2">
          <a href="/auth/login" class="btn btn-ghost btn-sm">Masuk</a>
          <a href="/auth/register" class="btn btn-primary btn-sm">Daftar</a>
        </div>
      <% } %>
    </div>

    <% if (currentUser) { %>
      <div id="nav-mobile" class="hidden md:hidden pb-4 space-y-1 animate-fadeUp">
        <a href="/courses" class="block px-3 py-2.5 rounded-lg text-sm font-medium <%= currentPath.startsWith('/courses') ? 'bg-brand-50 text-brand-700' : 'text-ink-700/80 hover:bg-ink-900/5' %>">Dasbor</a>
        <a href="/outlines" class="block px-3 py-2.5 rounded-lg text-sm font-medium <%= currentPath.startsWith('/outlines') ? 'bg-brand-50 text-brand-700' : 'text-ink-700/80 hover:bg-ink-900/5' %>">Outline</a>
        <a href="/chatbot/history" class="block px-3 py-2.5 rounded-lg text-sm font-medium <%= currentPath.startsWith('/chatbot') ? 'bg-brand-50 text-brand-700' : 'text-ink-700/80 hover:bg-ink-900/5' %>">Riwayat Chatbot</a>
        <a href="/profile" class="block px-3 py-2.5 rounded-lg text-sm font-medium <%= currentPath.startsWith('/profile') ? 'bg-brand-50 text-brand-700' : 'text-ink-700/80 hover:bg-ink-900/5' %>">Profil</a>
        <div class="flex items-center justify-between pt-2 mt-2 border-t border-ink-900/5">
          <span class="text-sm text-ink-700/60">Halo, <strong class="text-ink-900"><%= currentUser.name %></strong></span>
          <form action="/auth/logout" method="POST">
            <button type="submit" class="btn btn-secondary btn-sm">Keluar</button>
          </form>
        </div>
      </div>
    <% } %>
  </div>
</nav>
